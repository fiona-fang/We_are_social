# call this script from the command line like this:
# $ ./setup-libs.sh

# Install commands 
sudo apt-get -y install libjpeg-dev zlib1g-dev libmysqlclient-dev
# add more apt-get install lines below if you need more libs installed